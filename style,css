/* --- CONFIGURAÇÕES DE CORES --- */
:root {
    --bg-principal: #0B0B0B;
    --vermelho-vibrante: #E50914;
    --vermelho-escuro: #2B0608;
    --preto-card: #161616;
    --preto-input: #222222;
    --texto-branco: #FFFFFF;
    --texto-cinza: #A0A0A0;
    --verde-lucro: #00E676;
    --verde-escuro: #0A2512;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
}

body {
    background-color: var(--bg-principal);
    color: var(--texto-branco);
    display: flex;
    justify-content: center;
    min-height: 100vh;
}

.app-container {
    width: 100%;
    max-width: 480px;
    padding: 20px;
    padding-bottom: 100px; /* Um pouco mais de espaço no fim */
}

/* --- HEADER --- */
header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 24px;
}

header h1 {
    font-size: 16px;
    font-weight: 700;
    letter-spacing: 1.5px;
}

/* --- CARTÃO BALANÇO --- */
.balanco-header {
    display: flex;
    justify-content: space-between;
    color: var(--vermelho-vibrante);
    font-weight: 500;
    font-size: 13px;
    letter-spacing: 2px;
    margin-bottom: 12px;
}

.saldo-valor {
    font-size: 42px; 
    font-weight: 500;
    margin-bottom: 24px;
    letter-spacing: -1px;
}

.mini-info-container {
    display: flex;
    justify-content: space-between;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
    padding-top: 14px;
}

.mini-info p {
    font-size: 13px; 
    color: var(--texto-cinza);
    font-weight: 400;
    margin-bottom: 6px;
}

.mini-info span {
    font-size: 16px; 
    font-weight: 500;
}

.balanco-card {
    background-color: var(--vermelho-escuro);
    border: 1px solid rgba(229, 9, 20, 0.3);
    border-radius: 24px;
    padding: 24px;
    margin-bottom: 32px;
}

/* --- TRANSAÇÕES --- */
.transacoes-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 16px;
}

h2 {
    font-size: 18px;
    font-weight: 600;
}

.ver-todas {
    color: var(--vermelho-vibrante);
    text-decoration: none;
    font-size: 14px;
}

.transacao-item {
    background-color: var(--preto-card);
    border-radius: 16px;
    padding: 16px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 12px;
}

.transacao-info {
    display: flex;
    align-items: center;
    gap: 16px;
}

.transacao-icon {
    padding: 10px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
}

.transacao-icon.despespa {
    background-color: var(--vermelho-escuro);
    color: var(--vermelho-vibrante);
}

.transacao-icon.receita {
    background-color: var(--verde-escuro);
    color: var(--verde-lucro);
}

.transacao-detalhes h4 {
    font-size: 14px;
    font-weight: 500;
}

.transacao-detalhes p {
    font-size: 12px;
    color: var(--texto-cinza);
    margin-top: 4px;
}

.transacao-valores-acoes {
    display: flex;
    align-items: center;
    gap: 12px;
}

.transacao-valor {
    font-weight: 600;
    font-size: 15px;
    color: var(--vermelho-vibrante);
}
.transacao-valor.receita {
    color: var(--verde-lucro);
}

.btn-editar-item {
    background: none;
    border: none;
    color: var(--texto-cinza);
    cursor: pointer;
    display: flex;
    align-items: center;
}

/* --- ESTILO SEÇÃO DOS BANCOS --- */
/* --- ESTILO SEÇÃO DOS BANCOS --- */
.bancos-container {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

.banco-item {
    background-color: var(--preto-card);
    border-left: 3px solid var(--vermelho-vibrante);
    border-radius: 12px;
    padding: 14px 16px;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.banco-nome {
    font-size: 14px;
    font-weight: 500;
    color: var(--texto-branco);
}

/* Alterado para que todos os saldos de bancos fiquem verdes fixos */
.banco-saldo {
    font-size: 14px;
    font-weight: 600;
    color: var(--verde-lucro); /* Todos os bancos agora ficam verdes */
}

.banco-saldo.positivo {
    color: var(--texto-branco);
}

.banco-saldo.negativo {
    color: #ff4d5a;
}

/* --- GRAFICO + LEGENDA --- */
.relatorio-categorias-container {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background-color: var(--preto-card);
    border-radius: 24px;
    padding: 20px;
    border: 1px solid rgba(255, 255, 255, 0.05);
    gap: 16px;
}

.grafico-box {
    flex: 0 0 160px;
    display: flex;
    align-items: center;
    justify-content: center;
}

.tabela-container {
    flex: 1;
}

.tabela-categorias {
    width: 100%;
    border-collapse: collapse;
}

.tabela-categorias th {
    display: none;
}

.tabela-categorias td {
    padding: 10px 0;
    font-size: 13px;
    font-weight: 400;
    border-bottom: 1px solid rgba(255, 255, 255, 0.04);
}

.tabela-categorias tr:last-child td {
    border-bottom: none;
}

.legenda-cor-indicador {
    display: inline-block;
    width: 10px;
    height: 10px;
    border-radius: 50%;
    margin-right: 8px;
    vertical-align: middle;
}

/* --- NAV BAR --- */
nav {
    position: fixed;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 100%;
    max-width: 480px;
    background-color: var(--preto-card);
    height: 65px;
    display: flex;
    justify-content: space-around;
    align-items: center;
    border-radius: 20px 20px 0 0;
    z-index: 10;
}

.nav-item {
    background: none;
    border: none;
    color: var(--texto-cinza);
    cursor: pointer;
    display: flex;
    align-items: center;
    transition: color 0.2s;
}

.nav-item.ativo {
    color: var(--vermelho-vibrante);
}

.fab {
    background-color: var(--vermelho-vibrante);
    color: white;
    border: none;
    width: 56px;
    height: 56px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    position: absolute;
    top: -20px;
    left: calc(50% - 28px);
    box-shadow: 0 4px 10px rgba(229, 9, 20, 0.4);
}

/* --- MODAL --- */
.modal-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    background-color: rgba(0, 0, 0, 0.85);
    display: flex;
    justify-content: center;
    align-items: flex-end;
    z-index: 100;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.3s ease;
}

.modal-overlay.visivel {
    opacity: 1;
    pointer-events: auto;
}

.modal-container {
    width: 100%;
    max-width: 480px;
    background-color: var(--preto-card);
    border-radius: 24px 24px 0 0;
    padding: 24px;
    border-top: 1px solid rgba(229, 9, 20, 0.2);
}

.modal-container h3 {
    margin-bottom: 20px;
    font-size: 18px;
    font-weight: 600;
}

.form-group {
    display: flex;
    flex-direction: column;
    margin-bottom: 16px;
}

.form-group label {
    font-size: 12px;
    color: var(--texto-cinza);
    margin-bottom: 6px;
}

.form-group input, .select-input {
    background-color: var(--preto-input);
    border: 1px solid #333;
    padding: 14px;
    border-radius: 12px;
    color: white;
    font-size: 16px;
    outline: none;
    width: 100%;
}

.select-input {
    cursor: pointer;
    appearance: none;
    background-image: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='24' height='24' viewBox='0 0 24 24' fill='white'><path d='M7 10l5 5 5-5z'/></svg>");
    background-repeat: no-repeat;
    background-position: right 14px center;
}

.form-group input:focus, .select-input:focus {
    border-color: var(--vermelho-vibrante);
}

.tipo-botoes {
    display: flex;
    gap: 12px;
}

.btn-tipo {
    flex: 1;
    padding: 12px;
    border-radius: 12px;
    border: 1px solid #333;
    background: none;
    color: var(--texto-cinza);
    font-weight: bold;
    cursor: pointer;
}

.btn-tipo.despesa.ativo {
    background-color: var(--vermelho-escuro);
    border-color: var(--vermelho-vibrante);
    color: var(--vermelho-vibrante);
}

.btn-tipo.receita.ativo {
    background-color: var(--verde-escuro);
    border-color: var(--verde-lucro);
    color: var(--verde-lucro);
}

.btn-excluir {
    width: 100%;
    background-color: #3d0c11;
    color: #ff4d5a;
    border: 1px solid #7a1822;
    padding: 12px;
    border-radius: 12px;
    font-weight: bold;
    cursor: pointer;
    margin-top: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
}

.modal-actions {
    display: flex;
    gap: 12px;
    margin-top: 24px;
}

.modal-actions button {
    flex: 1;
    padding: 14px;
    border-radius: 12px;
    font-weight: bold;
    cursor: pointer;
    border: none;
}

.btn-cancelar {
    background-color: #222;
    color: white;
}

.btn-salvar {
    background-color: var(--vermelho-vibrante);
    color: white;
}
/* Estilos para as novas tabelas detalhadas de mês */
.tabela-detalhada-container tr {
    border-bottom: 1px solid rgba(255, 255, 255, 0.04);
}
.tabela-detalhada-container tr:last-child {
    border-bottom: none;
}
.tabela-detalhada-container td {
    padding: 12px 0;
}
